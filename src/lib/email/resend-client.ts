// Unused — superseded by the GHL-webhook-only flow in
// /src/app/api/assessment/submit/route.ts and /src/app/api/contact/submit/route.ts.
// This whole /src/lib/email directory is safe to delete; it's kept only
// because the environment this was authored in couldn't remove files.
export {};
